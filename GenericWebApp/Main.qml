import QtQuick
import QtQuick.Controls
import "AppConstants.qml"

ApplicationWindow {
    width: AppConstants.WINDOW_WIDTH
    height: AppConstants.WINDOW_HEIGHT
    title: AppConstants.TITLE
    visible: true
}
